"use client";

/**
 * Desktop search moved to SidebarNav («Поиск») + field on `/search`.
 * Mobile search uses MobileTabBar sheet — this bar is unused.
 */
export function ListingBottomBar() {
  return null;
}

/** @deprecated Use ListingBottomBar */
export const ListingMobileBottomBar = ListingBottomBar;
