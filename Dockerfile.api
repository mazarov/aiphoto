FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci

COPY tsconfig.json ./
COPY src ./src

RUN npm run build

# Copy font assets to dist (opentype.js loads .otf at runtime)
COPY src/assets dist/assets

EXPOSE 3001
CMD ["node", "dist/index.js"]
