---
description: Правила запуска скриптов на DigitalOcean (DO). Читай перед любой задачей с DO.
alwaysApply: true
---

# Запуск скриптов на DigitalOcean (DO)

## Что такое DO в контексте проекта

DO = DigitalOcean droplet. Там **нет развёрнутого проекта**, нет `npm install`, нет `node_modules`. Сервер используется только как раннер для одноразовых скриптов.

## Как запускать скрипты на DO

1. Скачать нужный скрипт напрямую с GitHub (raw):

```bash
curl -sO https://raw.githubusercontent.com/mazarov/aiphoto/main/src/<script-name>.ts
```

2. Если скрипт зависит от `src/lib/*` — скачать зависимости тоже:

```bash
curl -sO https://raw.githubusercontent.com/mazarov/aiphoto/main/src/lib/llm.ts
```

3. Env-переменные **уже сохранены на DO** как постоянные (в `.bashrc` / profile).
   НЕ нужно экспортировать каждый раз. НЕ включать `export ...` в инструкции.
   Если нужна НОВАЯ переменная — напомнить пользователю добавить её один раз.

4. Запускать через `node` (Node 20+ уже стоит на сервере) **только в фоне**:

```bash
nohup node <script-name>.mjs --dry-run > <script-name>.log 2>&1 &
```

## Альтернатива: standalone `.mjs` скрипты

Для DO лучше делать **standalone `.mjs`** версии скриптов:
- Без импортов из `src/lib/*` — всё inline
- Без `dotenv`, `@supabase/supabase-js` — только `fetch` (Node 20+)
- Хранятся в git-пути, доступном через raw URL (например `src/standalone/`)

## Правила

- **НЕ давать инструкцию `git clone` / `npm install`** на DO
- **НЕ предполагать** что на DO есть `package.json` или `node_modules`
- Каждый раз давать команду `curl` + `nohup node ... > log 2>&1 &`
- НЕ писать `export` секретов — они уже на сервере
- Секреты — НИКОГДА в коде, только env vars
- После команды запуска всегда давать команды проверки:
  - `ps aux | grep <script-name>`
  - `tail -f <script-name>.log`

## Когда пользователь пишет "запусти на DO"

1. Проверь, есть ли standalone `.mjs` версия скрипта в git-пути (предпочтительно `src/standalone/`)
2. Если нет — предложи сделать или дай `curl` инструкцию для `.ts` версии
3. НЕ писать `export` секретов (уже на сервере). Если нужна новая переменная — сказать один раз.
4. Всегда начинай с `--dry-run`
5. Все команды запуска — только в фоне (`nohup ... &`)
