---
description: Пайплайн загрузки новых карточек промтов из Telegram-экспорта в БД и на лендинг
alwaysApply: false
---

# Загрузка карточек промтов

## Обзор пайплайна

```
Telegram-экспорт → Parse → LLM Enrich (titles ru/en/de + prompt en/de) → Инжест в БД (с dedup) → LLM SEO-теги → Публикация
```

## Именование датасетов

Формат: `{SUPPLIER_KEY}_{Source}_{YYYY-MM-DD}`

| Часть | Значение | Пример |
|---|---|---|
| `SUPPLIER_KEY` | Ключ поставщика (канала) | `LEXYGPT` |
| `Source` | Откуда получили данные | `ChatExport` |
| `YYYY-MM-DD` | Дата, до которой забрали данные | `2026-03-13` |

Пример: `LEXYGPT_ChatExport_2026-03-13`

### Legacy-маппинг

Старые датасеты маппятся на supplier key через `SUPPLIER_LEGACY_MAP` в скрипте:

| Legacy slug | Supplier key |
|---|---|
| `lexy_15.02.26` | `LEXYGPT` |

## Шаг 0. Подготовка

Экспорты лежат в `docs/export/`. Каждая папка — датасет.

Узнать сколько карточек уже загружено из датасета:

```bash
cd /Users/azarovmaxim/photo2sticker-bot && node -r dotenv/config -e "
const { createClient } = require('@supabase/supabase-js');
const sb = createClient(process.env.SUPABASE_SUPABASE_PUBLIC_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } });
sb.from('prompt_cards').select('id', { count: 'exact', head: true }).eq('source_dataset_slug', 'DATASET_SLUG').then(r => console.log('Cards:', r.count));
"
```

## Шаг 1. Инжест карточек (с LLM-обогащением)

```bash
cd aiphoto
npx tsx src/ingest-telegram-export-to-supabase.ts \
  --dataset LEXYGPT_ChatExport_2026-03-13 \
  --limit 100
```

Скрипт автоматически выполняет **обогащение через LLM** перед записью в БД:
1. Parse HTML → карточки в памяти
2. Для каждой карточки — **один LLM-вызов** → SEO titles (ru/en/de) + перевод промпта (en/de)
3. Сохранение полной карточки в БД (titles + prompt translations)

- **Автоматический dedup** — скрипт определяет supplier key из slug, находит ВСЕ датасеты этого supplier, проверяет `source_message_id` по ним. Дубли пропускаются автоматически.
- `--offset` — опционально, для пагинации по новым карточкам (dedup уже отфильтровал старые)
- `--limit` — сколько новых загрузить за раз
- `--dry-run` — сначала проверить без записи в БД
- `--existing-only` — только перезаписать уже существующие карточки
- `--skip-llm` — пропустить LLM-обогащение (быстрый инжест, titles из парсера)
- `--concurrency N` — параллельность LLM-вызовов (default: 3)
- Скрипт использует `upsert`, повторный запуск безопасен

⚠️ **LLM-обогащение требует `OPENAI_API_KEY`** в env. Если ключ отсутствует — обогащение пропускается, карточка сохраняется с title из парсера.

### Вывод скрипта

```json
{
  "dataset": "LEXYGPT_ChatExport_2026-03-13",
  "supplierKey": "LEXYGPT",
  "supplierDatasets": ["LEXYGPT_ChatExport_2026-03-13", "lexy_15.02.26"],
  "skippedDuplicates": 228,
  "cardsSelected": 45,
  "parsedSuccess": 45,
  ...
}
```

## Шаг 2. SEO-теги (LLM)

Используется **OpenAI API** (gpt-4.1-mini по умолчанию) для классификации промтов.

```bash
# LLM-классификация новых карточек (seo_readiness_score = 0)
npx tsx src/fill-seo-tags.ts --dataset LEXYGPT_ChatExport_2026-03-13

# Пересчитать ВСЕ карточки датасета через LLM
npx tsx src/fill-seo-tags.ts --dataset LEXYGPT_ChatExport_2026-03-13 --recompute-all

# Regex fallback (если LLM недоступен)
npx tsx src/fill-seo-tags.ts --dataset LEXYGPT_ChatExport_2026-03-13 --regex-only
```

⚠️ **OpenAI и Gemini блокируют запросы из РФ.** Для LLM-тегирования использовать DO сервер (standalone-скрипт) или VPN.

### Как работает LLM-тегирование

- Модель: `gpt-4.1-mini` (OpenAI Chat Completions, JSON mode)
- Input: title + prompt_text_ru
- Output: `{ audience_tag: [...], style_tag: [...], ..., new_tags: [...] }`
- Параллельность: 5 запросов (configurable via `--concurrency`)
- Retry: 3 попытки с exponential backoff на 429/500/503
- Fallback: если LLM не отвечает → regex extraction

### Env переменные

- `OPENAI_API_KEY` — должен быть в `landing/.env.local` или `.env`
- `SUPABASE_SERVICE_ROLE_KEY` — для записи в БД
- `LLM_MODEL` — опционально (default: `gpt-4.1-mini`)

## Шаг 2.5. Проверка новых тегов (ОБЯЗАТЕЛЬНО)

После `fill-seo-tags` в выводе есть секция `🆕 New tags discovered`.

**Агент ОБЯЗАН спросить пользователя:**

> "LLM обнаружил N новых тегов. Теги с >= 3 вхождениями: [список]. Добавить в tag-registry.ts?"

Если пользователь подтверждает — добавить теги в `landing/src/lib/tag-registry.ts` с `urlPath` и `patterns`, затем в каталоге `landing` выполнить `npm run seo:sync` (дописывает шаблонные блоки в `seo-content.ts` для новых slug). Проверка покрытия: `npm run seo:check`.

## Шаг 3. Публикация

```bash
cd /Users/azarovmaxim/photo2sticker-bot && node -r dotenv/config -e "
const { createClient } = require('@supabase/supabase-js');
const sb = createClient(process.env.SUPABASE_SUPABASE_PUBLIC_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } });
sb.from('prompt_cards').update({ is_published: true }).eq('is_published', false).eq('source_dataset_slug', 'DATASET_SLUG').select('id').then(r => console.log('Published:', r.data?.length ?? 0));
"
```

## Добавление нового тега

Одна запись в `landing/src/lib/tag-registry.ts`:

```typescript
{ slug: "new_tag", dimension: "object_tag", labelRu: "Новый", labelEn: "New", urlPath: "/new-tag", patterns: [/паттерн/i] },
```

Автоматически заработает:
- LLM-классификация (`fill-seo-tags.ts` строит enum из TAG_REGISTRY)
- Regex fallback (через поле `patterns`)
- URL на лендинге (catch-all `[...slug]/page.tsx`)
- После коммита в реестр: `cd landing && npm run seo:sync` — запись в `seo-content.ts` (L1 title/FAQ/howTo по шаблону из `seo-content-from-tag.ts`)
- Для показа в меню — добавить `tagItem("new_tag")` в `landing/src/lib/menu.ts` (либо тег попадёт в группу «Ещё» по измерению)

## Добавление нового поставщика

1. Если legacy slug — добавить маппинг в `SUPPLIER_LEGACY_MAP` в `src/ingest-telegram-export-to-supabase.ts`
2. Новые датасеты — использовать формат `{SUPPLIER_KEY}_{Source}_{YYYY-MM-DD}` (key определяется автоматически)

## Типичный сценарий: новый экспорт от существующего поставщика

```bash
# 1. Анализ источника
npx tsx src/analyze-source.ts <slug>
# 2. Dry-run
npx tsx src/ingest-telegram-export-to-supabase.ts --dataset <slug> --dry-run
# 3. Инжест + LLM-обогащение (titles ru/en/de + prompt en/de — один LLM-вызов на карточку)
npx tsx src/ingest-telegram-export-to-supabase.ts --dataset <slug>
# 4. LLM SEO-теги (на DO сервере если из РФ!)
npx tsx src/fill-seo-tags.ts --dataset <slug>
# 5. ⚠️ Проверить новые теги → спросить пользователя → добавить в tag-registry.ts → npm run seo:sync в landing/
# 6. Публикация (SQL)
# 7. Проверить на лендинге
```

Шаг перевода `translate-en-prompts.ts` больше не нужен — переводы (en/de) создаются во время инжеста.
Скрипт `fix-prompt-marker-titles.ts` не нужен для новых карточек — SEO titles генерируются при инжесте.
Оба скрипта остаются для backfill старых карточек.
